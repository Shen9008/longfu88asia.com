# Cloudflare Pages — static site at repo root (no build).
# Deploy: npx wrangler pages deploy . --project-name=longfu88asia
# Dashboard project name: longfu88asia (longfu88asia.com)

name = "longfu88asia"
compatibility_date = "2024-01-15"
pages_build_output_dir = "."
